The creation of symlinks in the /vagrant-directory doesn't seem to work on Windows-Hosts! This is, why 'vagrant up' will only work "out of the box" on Linux-hosts!

( The symlinks are created during 'npm install'. )